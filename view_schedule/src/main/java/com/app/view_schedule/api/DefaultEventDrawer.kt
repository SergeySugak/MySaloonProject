package com.app.view_schedule.api

import android.graphics.Canvas
import android.graphics.RectF

class DefaultEventDrawer: EventDrawer {
    override fun draw(event: SchedulerEvent, canvas: Canvas, rect: RectF) {

    }
}